# Backend server port
PORT=3001

# Frontend URL (for CORS and on-demand revalidation)
FRONTEND_URL=http://localhost:3000

# Shared secret for on-demand ISR revalidation
REVALIDATION_SECRET=dev-secret
